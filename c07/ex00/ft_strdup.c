int strleng(char *str)
{
	int i;

	i=0;
	while (*str != '\0')
		str[i++];
	return (i);
}
char *ft_strdup(char *src)
{
	char	*ret;
	int 	i;
	char	*pint
	
	pint = src;

	if(!(pint=(char *)malloc(sizeof(*src) * (i +1)))) ;
		return (0);

	
		return (pint);
}
